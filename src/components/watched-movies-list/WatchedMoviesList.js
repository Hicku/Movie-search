import React from "react";
import "./watched-movies-list.css";
import WatchedMovie from "../watched-movie/WatchedMovie";

export default function WatchedMoviesList() {
  return (
    <div className="watched-movies-list-container">
      {/* {initialWatched.map((movie) => (
        <card>
          <WatchedMovie movie={movie} />
        </card>
      ))} */}
    </div>
  );
}
